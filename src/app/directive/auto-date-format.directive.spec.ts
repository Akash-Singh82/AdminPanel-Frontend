import { AutoDateFormatDirective } from './auto-date-format.directive';

describe('AutoDateFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoDateFormatDirective();
    expect(directive).toBeTruthy();
  });
});
